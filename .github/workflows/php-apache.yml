name: PHP Apache - Build and Push Image

on:
  workflow_dispatch:

jobs:
 
  build_and_push:
    uses: andrejvysny/workflows/.github/workflows/docker/docker_build_push_ghcr.yaml@main
    with:
      context: ./php/php-apache/
      tags: ghcr.io/andrejvysny/php-apache:8.1
    secrets:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}